## Steps to setup mysql db for Icinga Web2

$ mysql -u root -p -h <%= @db_host -%> -P <%= @db_port -%>

mysql> CREATE DATABASE <%= @db_name -%>;

mysql> GRANT SELECT, INSERT, UPDATE, DELETE, DROP, CREATE VIEW, INDEX, EXECUTE ON <%= @db_name -%>.* TO '<%= @db_user -%>'@'%' IDENTIFIED BY '<%= @db_password -%>';

mysql> FLUSH PRIVILEGES;

$ mysql -u root -p -h <%= @db_host -%> -P <%= @db_port -%> <%= @db_name -%> < <%= @schema_file -%>
